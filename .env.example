# ===========================================
# TarsChat — Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values.
# Do NOT commit .env.local to git.

# --- Clerk (Authentication) ---
# From Clerk Dashboard → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# Optional: custom routes (defaults work if not set)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat

# Required for Clerk → Convex user sync (webhook)
# From Clerk Dashboard → Webhooks → Add endpoint → Copy "Signing Secret"
# Endpoint URL: https://your-domain.com/api/webhooks/clerk
# Events: user.created, user.updated, user.deleted
CLERK_WEBHOOK_SECRET=whsec_xxxxx

# --- Convex (Backend & Realtime) ---
# From Convex: run `npx convex dev` and copy the URL it prints
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Optional: for `npm run build` (and CI). Get from Convex Dashboard → Settings → Deploy Key.
# If not set, run `npx convex dev` once to log in, then build will work.
# CONVEX_DEPLOY_KEY=prod:your-deployment|eyJ...

# --- Convex Dashboard (not in .env.local) ---
# Set in Convex Dashboard → Settings → Environment Variables:
#   CLERK_JWT_ISSUER_DOMAIN = Issuer URL from Clerk JWT Template (Convex)
